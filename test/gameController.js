/* global it contract */
// var GameController = artifacts.require('./GameController.sol')

contract('GameController', function (accounts) {
  it('first game at 12am on 26 January 2018 AEST', function () {
    // todo
    // let currentTime
    // return GameController.deployed().then(function (instance) {
    //   return instance.getGame.call(currentTime);
    // }).then(function (gameId) {
    //   assert.equal(balance.valueOf(), 10000, "10000 wasn't in the first account");
    // });
  })

  it('game closes after 10mins (no more bets)', function () {
    // todo
    // let currentTime
    // return GameController.deployed().then(function (instance) {
    //   return instance.getGame.call(currentTime);
    // }).then(function (gameId) {
    //   assert.equal(balance.valueOf(), 10000, "10000 wasn't in the first account");
    // });
  })

  it('unmatched bets are returned ???', function () {
    // todo
    // let currentTime
    // return GameController.deployed().then(function (instance) {
    //   return instance.getGame.call(currentTime);
    // }).then(function (gameId) {
    //   assert.equal(balance.valueOf(), 10000, "10000 wasn't in the first account");
    // });
  })

  it('can place a heads bet for a game while game is open', function () {
    // todo
  })

  it('can match a heads bet for a game while game is open', function () {
    // todo
  })
})
